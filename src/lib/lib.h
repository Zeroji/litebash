#ifndef _LIB_H
#define _LIB_H

// `cat` function. Copies stdin to stdout.
int cat(int argc, char *argv[]/*, FILE* in, FILE* out, FILE* err*/);

// `rev` function. Reverse stdin to stdout.
int rev(int argc, char *argv[]/*, FILE* in, FILE* out, FILE* err*/);

#endif /* _LIB_H */
